# Copy this file to .env and set your values.
# .env is gitignored — never commit real passwords.

POSTGRES_PASSWORD=changeme
# POSTGRES_USER=pyra     # optional, defaults to pyra
# POSTGRES_DB=pyra       # optional, defaults to pyra
